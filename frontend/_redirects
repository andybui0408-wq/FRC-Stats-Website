# Netlify redirects file (backup)
# This file is placed in the publish directory

# API routes go to serverless function
/api/* /.netlify/functions/server 200

# SPA routing - all other routes serve index.html
/* /index.html 200
